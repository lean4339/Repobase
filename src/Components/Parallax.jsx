export const Parallax = (props) => {
    // eslint-disable-next-line react/prop-types
    const {clase} = props;
    return (
        <>
            <div className={clase}>
            </div>
        </>
    )
}